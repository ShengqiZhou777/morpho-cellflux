[project]
name = "morpho-cellflux"
version = "0.1.0"
description = "CellFlux-style conditional marker phenotype transport for Perturb-Multi hepatocyte images."
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
dependencies = [
  "anndata",
  "numpy",
  "pandas",
  "pyarrow",
  "pyyaml",
  "torch",
  "torchvision",
  "flow-matching",
  "torchdiffeq",
  "torchmetrics",
  "pytorch-lightning",
  "scikit-learn",
  "scipy",
  "matplotlib",
  "pillow",
  "tqdm",
]

[project.optional-dependencies]
cluster = ["submitit"]

[tool.setuptools.packages.find]
where = ["src"]
