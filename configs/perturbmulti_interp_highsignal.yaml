# Perturb-Multi hepatocytes -- interpolation figure subset for high-signal genes.
# Same data as perturbmulti_highsignal_id, used by scripts/interpolate_cellflux.sh
# so the first eval batch lands on genes with visible panel2 effects.
task_name: perturbmulti_interp_highsignal

ood_set: Null
mol_list: Null
trainable_emb: False
dataset_name: 'perturbmulti'
n_channels: 3
multimodal: False
batch_correction: False
batch_key: Null
use_condition_embeddings: False
add_controls: False
condition_embedding_dimension: null
modality_list:
  - Gene

image_path: data/raw/extracted_images
data_index_path: data/processed/cellflux_ext/index_highsignal.csv
embedding_path: data/processed/cellflux_ext/embedding_gene_identity.csv

augment_train: False
normalize: False
