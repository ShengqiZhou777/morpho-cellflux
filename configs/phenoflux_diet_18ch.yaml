# Diet hepatocytes — Information control (18ch naive concat).
# Same 18ch MERFISH marker profile as MSA, but consumed via naive
# mean-pool + concat instead of learned self-attention.
# Answers: does MSA matter, or just having the extra 18ch info?
task_name: diet_id
ood_set: null
mol_list: null
trainable_emb: false
dataset_name: perturbmulti
dataset: phenoflux
n_channels: 3
channels: [9, 5, 10]
multimodal: false
batch_correction: false
batch_key: null
use_condition_embeddings: false
add_controls: false
condition_embedding_dimension: null
modality_list:
  - Gene
image_path: data/raw/diet/images
data_index_path: data/processed/diet/index_diet.csv
embedding_path: data/processed/diet/embedding_diet.csv
use_marker_profile: true
augment_train: true
normalize: false
# PhenoFlux molecular prior
base_condition_dim: 3
use_marker_profile: true
